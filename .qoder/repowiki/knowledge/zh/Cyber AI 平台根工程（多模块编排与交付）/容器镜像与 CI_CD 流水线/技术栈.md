Docker 多阶段构建（Maven 3.9.9 + Temurin JDK 21 / JRE 21）；Logstash 文件输入 + JSON codec + Elasticsearch 输出；GitHub Actions（actions/checkout@v4、setup-java@v4、setup-node@v4、trivy-action）；Trivy 容器镜像安全扫描（CRITICAL/HIGH 级别阻断）。