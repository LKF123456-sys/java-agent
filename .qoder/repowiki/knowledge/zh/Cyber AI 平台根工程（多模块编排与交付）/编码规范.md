- 所有后端子模块继承根 `pom.xml` 父 POM，统一使用 `<properties>` 声明的 Java 21、Spring Boot 3.4.x、Spring AI 1.1.x 版本，禁止子模块自行覆盖核心版本。
- 生产环境配置一律通过 `SPRING_PROFILES_ACTIVE=docker` 与环境变量注入，禁止硬编码敏感信息（如 JWT_SECRET、TAVILY_API_KEY）。
- 容器化遵循多阶段构建：构建阶段使用 `maven:3.9.9-eclipse-temurin-21`，运行阶段仅包含 `eclipse-temurin:21-jre`，并将 `/app/logs` 与 `/app/uploads` 暴露为卷。