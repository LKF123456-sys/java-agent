本地开发需先 `ollama serve` 并拉取 `qwen2.5:7b`、`nomic-embed-text`，再依次执行 `mysql -u root -p` 建库、`mvn spring-boot:run`、`cd frontend && npm install && npm run dev`；Docker 部署默认走 PostgreSQL+PgVector，需 GPU 场景才启用 `--profile ollama`。