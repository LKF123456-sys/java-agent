- 实现计划使用 Markdown 复选框 `- [ ] Step N:` 作为可追踪的任务单元，每个 Step 附带 Run/Expected/Commit 三段式说明，便于 agentic worker 逐项执行。
- API 与配置变更在计划中同时给出完整代码片段与对应的 `git add ... && git commit -m "..."` 命令，确保提交信息遵循 feat/refactor/fix 前缀约定。